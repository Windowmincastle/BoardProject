# Starting point는 application.yml 파일이다.
spring:
  profiles:
    active: dev

#추후 jar 실행시
#아래와 같이 실행 시 애플리케이션야뮬의 설정을 덮어쓰기하는 것
# java "-Dspring.profiles.active=prod"-jar <jar파일명>



#spring:
#  datasource:
#    driver-class-name: org.mariadb.jdbc.Driver
#    url: jdbc:mariadb://localhost:3306/board
#    username: root
#    password: 1234
#  jpa:
#    database: mysql
#    database-platform: org.hibernate.dialect.MariaDBDialect
#    generate-ddl: true
#    hibernate:
#      ddl-auto: update
#    show_sql: true
#logging:
#  level:
##   로그 레벨 : trace < debug < info < error 순서의 로그 레벨을 가진다.
##   즉, info로 설정 시 trace, debug 로그는 출력되지 않는다.
#    root: info
#  file:
#    name: logs/app.log